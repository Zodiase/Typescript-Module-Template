{
  "name": "some-module",
  "version": "0.1.0",
  "description": "some description",
  "main": "lib/index.js",
  "engines": {
    "node": ">=6.13.0"
  },
  "scripts": {
    "start": "npm run build:dev-watch",
    "build:dev-watch": "tsc --watch",
    "test:dev-watch": "mocha --require ts-node/register --watch --watch-extensions ts,spec.ts src/**/*.spec.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xingchen Hong",
  "dependencies": {
  },
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.48",
    "@types/node": "^8.9.4",
    "chai": "^4.1.2",
    "mocha": "^4.1.0",
    "ts-node": "^4.1.0",
    "typescript": "^2.7.1"
  }
}
